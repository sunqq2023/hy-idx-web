import React from 'react'

interface CompProps {
    title: string
}

const useTitle = ({ title }: CompProps) => {
    const text = () => {}

    return {
        text
    }
}

export default useTitle
